<main class="main">
  <div class="inner">
   <% if user_signed_in? %>
    <%# ログインしているときは以下を表示する %>
     <div class="greeting"> 
      <%# <div class="greeting"> %>
        <%= "こんにちは、" %>
        <%# こんにちは、 %>
        <%= link_to "#{current_user.name}さん", "/users/#{current_user.id}", class: :greeting__link%>
        <%# <%= link_to "ユーザー名さん", root_path, class: :greeting__link%>
     </div>
      <%# </div> %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%= render partial: 'prototype', locals: { prototype: @prototype }, collection: @prototypes %>
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
   <% end %>
  </div>
</main>
